name: megalinter
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch: {}

jobs:
  lint:
    uses: chirag127/workflows/.github/workflows/megalinter.yml@main
    permissions:
      contents: read
      pull-requests: write
      issues: write
      security-events: write
